# Redirects for Next.js on Cloudflare Pages

# Proxy API requests to backend (force rewrite)
/api/* https://api.verbfy.com/api/:splat 200!

# SPA fallback – send everything else to index.html
/* /index.html 200
